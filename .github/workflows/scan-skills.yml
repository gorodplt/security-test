name: Scan Skills

on:
  push:
    paths: ["plugins/**"]
  pull_request:
    paths: ["plugins/**"]

jobs:
  scan:
    uses: cisco-ai-defense/skill-scanner/.github/workflows/scan-skills.yml@main
    with:
      skill_path: plugins
    permissions:
      security-events: write
      contents: read
