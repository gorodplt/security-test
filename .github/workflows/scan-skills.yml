jobs:
  scan:
    uses: cisco-ai-defense/skill-scanner/.github/workflows/scan-skills.yml@main
    with:
      skill_path: plugins
      use_llm: true
      use_behavioral: true
      policy: permissive
    secrets:
      llm_api_key: ${{ secrets.OA }}
      virustotal_api_key: ${{ secrets.VT }}
